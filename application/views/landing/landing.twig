<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
  
  <title>PickArtYou</title>
  
  <link rel="stylesheet" type="text/css" href="../../static/dist/css/semantic.min.css">
  <link rel="stylesheet" type="text/css" href="../../static/dist/css/themes.min.css">
  <script src="../../static/dist/js/jquery.min.js"></script>
  <script src="../../static/dist/js/semantic.min.js"></script>
  <style type="text/css">
    body {
      background-color: #DADADA;
    }
    body > .grid {
      height: 100%;
    }
    .image {
      margin-top: -100px;
    }
    .column {
      max-width: 450px;
    }
  </style>
</head>
<body>
  <div class="ui middle aligned center aligned grid">
    <div class="column">
      <h2 class="ui teal image header">
        <div class="content">
          Pickartyou Landing Page
        </div>
      </h2>
      <form id="landing_form" class="ui large form">
        <div class="ui stacked segment">
          <div class="field">
            <div class="ui left icon input">
              <i class="user icon"></i>
              <input type="text" name="email" placeholder="E-mail address">
            </div>
          </div>
        </div>
        <button class="ui fluid large teal submit button" type="submit">Login</button>
      </form>
      
      <div class="ui message">
        blah blah blah
      </div>
    </div>
  </div>
  <script>
    $('#landing_form').submit(function () {
      var emailElem = $("input[name=email]");
      
      if (!emailElem.val()) {
        alert('이메일 주소를 입력해주세요.');
        emailElem.focus();
        return false;
      }
      
      data = {};
      data.email = emailElem.val();
    
      $.ajax({
        method: "POST",
        url: "/api/landing",
        data: data
      }).done(function (data) {
        alert(data.body.message);
      }).fail(function () {
        alert('서버에 문제가 있어 정상적으로 처리되지 않았습니다. \r\n잠시 후에 다시 시도해주세요.');
      });
    
      return false;
    });
  </script>
</body>
</html>
